if(${IDF_VERSION_MAJOR} STREQUAL 6)
    set(requires esp_driver_gpio)
else()
    set(requires driver)
endif()
idf_component_register(SRCS "zh_cd74hc4067.c" INCLUDE_DIRS "include" REQUIRES ${requires})